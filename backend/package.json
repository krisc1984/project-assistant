{
  "name": "project-assistant-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src -e ts --exec ts-node src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.5.2",
    "dotenv": "^16.0.3",
    "jsonwebtoken": "^9.0.0",
    "exceljs": "^4.7.0",
    "pdfkit": "^0.12.3"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.1",
    "nodemon": "^2.0.22",
    "@types/node": "^20.6.2",
    "@types/express": "^4.17.21",
    "vitest": "^0.36.0",
    "supertest": "^6.3.3",
    "@types/supertest": "^2.0.12"
  },
  "scripts": {
    "dev": "nodemon --watch src -e ts --exec ts-node src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run --passWithNoTests",
    "migrate": "node ./scripts/migrate.js",
    "migrate-status": "node ./scripts/migrate_status.js",
    "migrate:dry": "node ./scripts/migrate.js --dry-run"
  }
}
