Phase 2 API Reference — OpenAPI Skeleton

- Endpoints scaffold:
  - /api/reports/templates (list templates)
  - /api/reports/templates/:id/export?format={csv|pdf|xlsx} (export placeholder)
- Health and scores placeholders:
  - /api/health
  - /api/projects
  - /api/projects/:id/scores
- RBAC and authentication notes
- Migration runner endpoints (internal use): /api/migrations/status
